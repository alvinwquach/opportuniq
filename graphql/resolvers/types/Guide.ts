/**
 * DIYGuide Type Resolvers
 *
 * Field resolvers for the DIYGuide type.
 * Most fields are direct mappings, minimal custom resolvers needed.
 */

import type { DIYGuide as DIYGuideType } from "@/app/db/schema";

export const DIYGuide = {
  // All fields are direct mappings from database
  // Add custom resolvers here if needed in the future
};
