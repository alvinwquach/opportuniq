/**
 * GraphQL Schema Assembly
 *
 * Combines type definitions with resolvers to create executable schema.
 * This is what GraphQL Yoga uses to handle requests.
 */

export { typeDefs } from "./typeDefs";
